type Query {
    getProducts: [Product]
}

type Mutation {
    createProduct(productName: String!,productType: ProductType!): Product
}

type Product {
    id: String,
    productName: String!,
    productType: ProductType!
}

enum ProductType {
    HAZARDOUS,
    STANDARD
}