type Query {
    getProducts: [Product]
}

type Mutation {
    createProduct(productName: String!,productType: String!): Product
}

type Product {
    id: String,
    productName: String!,
    productType: ProductType!
}

enum ProductType {
    HAZARDOUS,
    STANDARD
}